class FoodItemInfo {
  String image;
  String name;
  bool isVeg;
  String price;

  FoodItemInfo(this.image, this.name, this.isVeg, this.price);
}

class SecenekModel {
  String? secenek;
  bool? selected;

  SecenekModel(this.secenek, this.selected);
}
